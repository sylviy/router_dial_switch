# <设备> —— <任务> 适配

> **这是模版。** 做一个新的 UI 操作单元时,把这份整个拷到产出目录里改;
> 已经有像的单元了,就拷那一个 —— 拷来的那份是**填好的**,比空模版省事。
>
> 拷去哪里:
> * 拨号切换 + 性能 → `Scene/router_dial_switch/Models/<品牌>_<型号>/SKILL.md`
> * 单纯的 UI 操作 → `Scene/web_action/Devices/<品牌>_<型号>/<任务名>/SKILL.md`
>
> **只有第一部分是这台设备/这个任务特有的,二到六部分拷过去别改。**
> 重复是刻意的:改第八个绝不可能弄坏前七个,而且拷过去那份自足,
> 不用跳到上一层读另一半。

**产出是一个动作,不是一套测试。** 它只负责"把设备切到某个状态,并且证明真的
切到了"。

---

## 第一部分 · 任务表(适配人员填这里)

**用界面上的话写,不要写选择器。** 你看见按钮上写着 "Save",就写 "Save";
它在页面里叫 `input[name='save_apply']` 是 agent 去查的事,不是你要操心的。

写不清楚的地方就标 `待真机确认`,别猜 —— 猜错了比空着更糟,因为读的人会以为
这是验过的。

```yaml
设备:
  品牌: Mercusys
  型号: MR80X
  管理地址: http://192.168.1.1
  密码: admin123          # 或者写"见 config.yaml 的 router.pass"

任务:
  名字: vpn_server_switch
  说明: 在 VPN 页切换 Server 协议,填好各协议必填字段并保存

# ============ 第一段 · 前置(每次开工都要先走一遍的路)============
前置:
  - 怎么做: 登录界面中 "Local Password:" 填写 "admin123",再点击 "LOGIN" 登录按钮
    做完的样子: 登录成功后顶部菜单栏有 "Network Map" "Internet" "Wireless" "Advanced"
  - 怎么做: 点顶部菜单 "Advanced"
    做完的样子: 左侧出现 "VPN Server" 选项
  - 怎么做: 点左侧 "VPN Server"
    做完的样子: 下方展开 "OpenVPN" "PPTP" "L2TP/IPSec" "WireGuard" "Connections"

# ============ 第二段 · 状态表(一个状态一段)============
状态表:
  - 状态名: pptp_server
    左侧 "VPN Server" 下展列表中的选项文字: PPTP
    选完之后出现: "Enable" 复选框 和 "Add" 按钮
    要做什么:
      "Enable" 旁的复选框:做成「勾上」,小方框里出现对勾
      "Add" 按钮点击后跳出弹框 [Username, Password, CANCEL, ADD]
      "Username" "Password" 均填入 "test"
      再点击 "ADD"
    刷新后回读: 重新进 PPTP 页,"Enable" 旁复选框是勾上的

  - 状态名: l2tp_server
    注意: 整体架构类似于 pptp_server,复用上轮结论
    左侧 "VPN Server" 下展列表中的选项文字: L2TP/IPSec
    选完之后出现: "Enable" 复选框 和 "Add" 按钮
    要做什么:
      同 pptp_server(勾上 "Enable"、Add 弹框填 test/test → ADD)
    不同:
      勾上后页面多出 "Client IP Address" "IPSec Encryption" "IPSec Pre-Shared Key"
      "IPSec Pre-Shared Key" 填入 "test"
    刷新后回读: 重新进 L2TP/IPSec 页,"Enable" 旁复选框是勾上的,
      且 "IPSec Pre-Shared Key" 里是 "test"

# ============ 第三段 · 收尾(每个状态都走一遍)============
收尾:
  - 怎么做: 点 VPN 这个表单右下角的 "Save"
    做完的样子: 页面刷新完成
  - 怎么做: 刷新管理页面,重新导航进 VPN 界面
    做完的样子: 显示的值 == 这一行的「刷新后回读」

# ============ 安全 ============
会不会让我连不上这台设备: 否
每个状态都会真正保存: 是
```

**选择器不写在这里。** agent 探出来的选择器落在产出文件里(`facts.yaml`,
拨号那条路是型号脚本的 `FACTS`),那是唯一真相。这张表和它对不上时,
以那边为准,并把这张表改过来。

---

## 第二部分 · 规矩(不要改)

这几条是这套东西存在的理由。全部是真机上踩出来的,不是理论。

1. **点完不算数,刷新之后读回来才算数。**
   不刷新,你读到的是自己刚填进去的值,等于自己给自己打分。
   真机上出过:回读通过、实际提交的是旧值。
   **这一步 agent 自己做,不需要人来确认。**

2. **回读要一模一样,不能"差不多"。**
   目标是 PPPoE,页面显示 PPPoEv6,这不叫成功。差一个字就是失败。
   判定只有一个出口:`Vendor/common/contract.py` 的 `verify()`。

3. **页面上只有一个,不代表它就是对的。**
   真机上出过:同一页 4 个一模一样的保存按钮、8 个藏起来的提交按钮、
   一个人眼永远看不见但点得动的勾选框。所以每个选择器都要数过恰好 1,
   并在产出里写明是怎么认出它的。

4. **可以直接操作设备,包括保存。**
   适配时台架是断网的,WAN 口也不接出口 —— 点错了不会把人关在门外。
   所以不用停下来等人点头。

5. **和任务表对不上时,报告回来,不要自己猜。**
   任务表是人写的,人写错的可能性远大于设备不一样。绕路找补是最贵的做法。

6. **不猜没观察过的东西。** 每个选择器都得是探出来的,不是想出来的。

7. **`_pause` / `_find` / `_find_text` 这三个函数不许改。**
   它们在 `Tools/_probe.py` 里,全仓库唯一一份。探针可以整个复制到本目录的
   `tools/` 下自由编辑 —— 加控件形态、改输出都行 —— 但这三个原样保留。
   它们是"工具说命中 1"能预测"脚本也命中 1"的唯一依据。

---

## 第三部分 · 手上有哪些工具,怎么用它们推进

工具都在仓库根的 `Tools/` 下,**不要自己另写探测脚本**。
下面说的是每个工具**替你做什么**;命令行参数和边界情况在 `Tools/probing.md`。

| 工具 | 它替你做什么 | 什么算过 |
|---|---|---|
| `env_check.py` | 开工前的三项体检:解释器对不对、浏览器起不起得来、设备地址通不通 | 三项全绿。不绿是**环境问题,不是适配问题** —— 如实报告,别去改脚本 |
| `probe_dump.py` | **把当前这一页上能点的东西全抄下来**:每行一个控件,写着它是什么类型(下拉/单选/输入框/按钮)、上面显示什么字、有哪些能指认它的属性、在哪个文档里。回答"这一页到底有什么" | 清单里能对上任务表说的那个控件 |
| `list_modes.py` | 把一个下拉或单选组的**所有选项照原文抄出来**。回答"这个下拉里有哪几档、每一档界面上写的到底是什么字" | 选项数 ≥ 2,而且任务表里的措辞逐字来自这里 |
| `probe_count.py` | 数一个选择器**命中几个**。回答"我说的这个东西,页面上是不是只有一个" | **恰好 1**。多了说明这个说法还不够准 |
| `act.py` | **做一个动作,再回读证明它成了**:选中 / 勾上 / 填进去 / 点下去,然后把控件现在的值读回来,和目标逐字比对 | 回读 == 目标 |

`act.py` 上有三个开关值得单说,它们直接对应任务表里的三样东西:

* `--expect-after "<那个东西>"` ←→ 任务表的「**做完的样子**」。
  做完之后它要出现,这一步才算成立。**用它代替固定等待** —— 页面是逐步展开
  的,等多久没人说得准,但"那个东西出现了"是看得见的。
* `--reload-verify` ←→ 任务表的「**刷新后回读**」。
  刷新页面、重新登录、重走一遍菜单,再读一次。这是判"真的保存住了"还是
  "只是自己刚填的值还留在框里"的唯一办法。
* `--apply-sel "<保存键>"` ←→ 任务表的「**收尾**」。给了就真点保存。

`probe_dump.py` 的清单**只收表单元素**。页面上有、清单里没有,是常事 ——
真机上大量控件不是表单元素(比如图标做的开关,真值藏在旁边的隐藏输入框里)。
遇到就去看 `Tools/probing.md` 的「找不到怎么办」第 2 招,别以为页面上没有。

### 照任务表推进的顺序

```
先跑一次 env_check.py,三项全绿再开工。

对任务表里的每一步:
  1. probe_dump.py  —— 看看现在这一页有什么
  2. 认出任务表说的那个控件,probe_count.py 数一下,必须恰好 1
  3. 是选择类的,list_modes.py 把选项原文抄下来,和任务表里的措辞对一遍
  4. act.py 做这个动作,--expect-after 确认「做完的样子」出现了
  5. 「做完的样子」不成立就**别往下走** —— 页面还没到那个状态,后面全是白找

每个状态都过完之后,最后一遍加上 --apply-sel 和 --reload-verify:
真保存 + 刷新后回读。这一遍过了,才谈得上把它落成脚本。
```

前面 1~4 步**不会改设备**(只读 + 选中,不点保存)。第 5 步起才真下发,
**从最安全的那个状态开始**。

卡在具体现象上(登录不进去、认错控件、选择器收不窄、试切不过),
按节查 `reference.md`,**按节读,别整篇读**。

---

## 第四部分 · 按需询问(不是关卡)

**默认自己往下走。** 每一步都有能自己验证的通过条件,不必逐步等人点头。

只有下面这三种情况停下来问 —— 因为它们**再探测也解决不了**:

* 同名控件有好几个而且都可见,任务表里没有能区分它们的话;
* 真机和任务表对不上(那一项根本不存在、或者选项文字不一样);
* 一个动作可能顺带改到任务表没提的别的设置。

问的时候**用设备的话问,不要用选择器语言问**。照这个形状:

> 在「Advanced → VPN Server」页,我认的是:
> Enable = "Enable" 标签右边那个复选框(现在没勾);
> 保存 = 这个表单右下角的 "Save"。
> 同一页另外还有 2 个同样写着 "Save" 的按钮,任务表没说是哪个。
> **请确认:保存是不是右下角这一个?**

---

## 第五部分 · 产出是什么

一个目录,自足 —— 整个拷走就能跑:

```
<产出目录>/
    SKILL.md       就是本文件(填好的)
    action.py      这个动作本身,可以单独跑
    facts.yaml     探出来的控件位置,每个旁边一行注释说明是怎么认出来的
    tools/         可选:从 Tools/ 复制来、按这台设备改过的探针
```

命令行、stdout 那行 JSON、退出码 0/2/3 的完整契约在 `Tools/probing.md` 的
「产出契约」一节。

(拨号切换那条路的产出形状不一样 —— 是 `Models/<品牌>_<型号>/` 里一个自足的
型号脚本。拷型号那边的 SKILL.md 时,这一部分已经是写好的。)

---

## 第六部分 · 做完之后必须做的一件事

**把设备恢复到默认状态,再让脚本从头跑一遍。**

第一次跑通的时候,设备往往已经被你手工点到目标状态附近了。这时候的"成功"
可能是假的 —— 脚本其实什么都没做对,只是状态本来就对。

从干净状态跑通,是唯一能区分这两者的办法。跑不过就是还没做完。
