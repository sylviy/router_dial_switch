# Embedded offline runtime (150 MB) — rebuild per WINDOWS_离线部署.md:
#   vendor/python/  = Python 3.8 Windows embeddable zip, unpacked
#   vendor/wheels/  = pip download -r requirements.txt -d vendor/wheels
vendor/

# local run defaults incl. admin/broadband passwords (cli.py setup)
router.yaml

# models/ 层运行截图(engine 的 dial_*.png 样例仍保留在仓库里)
artifacts/model_*.png

__pycache__/
*.pyc
.DS_Store
venv/
.venv/
